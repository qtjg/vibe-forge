## Summary

<!-- What does this PR do, and why? Link the issue it closes, if any. -->

## Checklist

- [ ] Tests added/updated for new or changed behavior (`pytest` passes)
- [ ] `ruff check .` is clean
- [ ] `ruff format --check .` is clean
- [ ] Docs updated if behavior changed (README, CONTRIBUTING, skill file,
      API contract in `vibeforge/dashboard/app.py`)
- [ ] Benchmark task ids are unique and the suite stays 30–50 tasks (if
      `benchmark/tasks.py` changed)
- [ ] No live-Ollama dependency added to the test suite

## Notes for reviewers

<!-- Migration concerns, design trade-offs, CSV column changes, ... -->